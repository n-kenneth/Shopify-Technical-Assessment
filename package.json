{
  "name": "technical-asssesment",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "scss:watch": "sass --watch --no-source-map scss:assets",
    "scss:build": "sass --style=compressed --no-source-map scss:assets",
    "dev": "npm run scss:build && run-p -sr \"shopify:dev -- {@}\" \"scss:watch\" --",
    "build": "npm run scss:build",
    "shopify:dev": "shopify theme dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "npm-run-all": "^4.1.5",
    "sass": "^1.94.2"
  }
}
